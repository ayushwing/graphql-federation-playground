spring:
  application:
    name: user-subgraph

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

dgs:
  graphql:
    path: /graphql
    schema-locations: classpath:schema/**/*.graphqls
