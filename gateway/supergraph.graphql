# This file is generated by Apollo Rover — do not edit by hand.
# Regenerate with:
#   rover supergraph compose --config gateway/supergraph.yaml > gateway/supergraph.graphql
#
# Run `rover supergraph compose` after all subgraphs are running locally.

schema
  @link(url: "https://specs.apollo.dev/link/v1.0")
  @link(url: "https://specs.apollo.dev/join/v0.3", for: EXECUTION)
{
  query: Query
  mutation: Mutation
  subscription: Subscription
}

directive @join__enumValue(graph: join__Graph!) repeatable on ENUM_VALUE
directive @join__field(graph: join__Graph, requires: join__FieldSet, provides: join__FieldSet, type: String, external: Boolean, override: String, usedOverridden: Boolean) repeatable on FIELD_DEFINITION | INPUT_FIELD_DEFINITION
directive @join__graph(name: String!, url: String!) on ENUM_VALUE
directive @join__implements(graph: join__Graph!, interface: String!) repeatable on OBJECT | INTERFACE
directive @join__type(graph: join__Graph!, key: join__FieldSet, extension: Boolean! = false, resolvable: Boolean! = true, isInterfaceObject: Boolean! = false) repeatable on OBJECT | INTERFACE | UNION | ENUM | INPUT_OBJECT | SCALAR
directive @join__unionMember(graph: join__Graph!, member: String!) repeatable on UNION
directive @link(url: String, as: String, for: link__Purpose, import: [link__Import]) repeatable on SCHEMA

scalar join__FieldSet
scalar link__Import
enum link__Purpose { SECURITY EXECUTION }

enum join__Graph {
  USERS @join__graph(name: "users", url: "http://user-subgraph:8081/graphql")
  PRODUCTS @join__graph(name: "products", url: "http://product-subgraph:8082/graphql")
  REVIEWS @join__graph(name: "reviews", url: "http://review-subgraph:8083/graphql")
}

# ── User ──────────────────────────────────────────────────────────────────────

type User
  @join__type(graph: USERS, key: "id")
  @join__type(graph: PRODUCTS, key: "id", extension: true)
  @join__type(graph: REVIEWS, key: "id", extension: true)
{
  id: ID!
  username: String! @join__field(graph: USERS)
  email: String! @join__field(graph: USERS)
  name: String! @join__field(graph: USERS)
  bio: String @join__field(graph: USERS)
  avatarUrl: String @join__field(graph: USERS)
  createdAt: String! @join__field(graph: USERS)
  products: [Product!]! @join__field(graph: PRODUCTS)
  reviews: [Review!]! @join__field(graph: REVIEWS)
}

# ── Product ───────────────────────────────────────────────────────────────────

type Product
  @join__type(graph: PRODUCTS, key: "id")
  @join__type(graph: REVIEWS, key: "id", extension: true)
{
  id: ID!
  name: String! @join__field(graph: PRODUCTS)
  description: String @join__field(graph: PRODUCTS)
  price: Float! @join__field(graph: PRODUCTS)
  category: String! @join__field(graph: PRODUCTS)
  inStock: Boolean! @join__field(graph: PRODUCTS)
  createdBy: User! @join__field(graph: PRODUCTS)
  createdAt: String! @join__field(graph: PRODUCTS)
  reviews: [Review!]! @join__field(graph: REVIEWS)
  averageRating: Float @join__field(graph: REVIEWS)
  reviewCount: Int! @join__field(graph: REVIEWS)
}

# ── Review ────────────────────────────────────────────────────────────────────

type Review @join__type(graph: REVIEWS) {
  id: ID!
  rating: Int!
  comment: String!
  author: User!
  product: Product!
  createdAt: String!
}

# ── Pagination ────────────────────────────────────────────────────────────────

type ProductConnection @join__type(graph: PRODUCTS) {
  nodes: [Product!]!
  totalCount: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

# ── Inputs ────────────────────────────────────────────────────────────────────

input ProductFilter @join__type(graph: PRODUCTS) {
  category: String
  minPrice: Float
  maxPrice: Float
  inStock: Boolean
  nameContains: String
}

input CreateReviewInput @join__type(graph: REVIEWS) {
  productId: ID!
  rating: Int!
  comment: String!
}

# ── Root types ────────────────────────────────────────────────────────────────

type Query {
  user(id: ID!): User @join__field(graph: USERS)
  users: [User!]! @join__field(graph: USERS)
  me: User @join__field(graph: USERS)
  product(id: ID!): Product @join__field(graph: PRODUCTS)
  products(filter: ProductFilter, page: Int = 0, size: Int = 20): ProductConnection! @join__field(graph: PRODUCTS)
  productsByCategory(category: String!): [Product!]! @join__field(graph: PRODUCTS)
  reviews(productId: ID!): [Review!]! @join__field(graph: REVIEWS)
  review(id: ID!): Review @join__field(graph: REVIEWS)
  myReviews: [Review!]! @join__field(graph: REVIEWS)
}

type Mutation {
  createReview(input: CreateReviewInput!): Review! @join__field(graph: REVIEWS)
  deleteReview(id: ID!): Boolean! @join__field(graph: REVIEWS)
}

type Subscription {
  reviewAdded(productId: ID!): Review! @join__field(graph: REVIEWS)
}
